# Days until binary expires from build time.
# -1 = no expiry (binary never expires).
# SAMGRAHA_EXPIRY_DAYS=30

# Additional hours on top of EXPIRY_DAYS.
# -1 = no hours component (treated as 0).
# SAMGRAHA_EXPIRY_HOURS=0

# Output directory for the release package.
# Use an absolute path — this file is machine-specific, so hardcode it.
# OUTPUT_DIR=C:\Users\you\releases\samgraha

# Absolute path for generated reports (e.g. `samgraha audit --report`).
# Unset falls back to <repo>/docs/raw/reports.
# SAMGRAHA_REPORT_DIR=

# Absolute path to this repository's implementation/source directory.
# Reserved for future traceability checks; unset falls back to <repo>/src.
# SAMGRAHA_IMPLEMENTATION_DIR=

# Copy this file to .env and uncomment the values to configure:
#   cp .env.example .env
