# Documentation Cleanup Proposal — Align `docs/raw/` with Current Architecture

**Status**: COMPLETE, verified against the filesystem (not just the
implementer's report). All 6 phases done: `docs/raw/audit/` and
`docs/raw/reports/` both confirmed removed, all 8 new capability-dispatch
docs confirmed present, zero remaining matches for `run_pipeline()`,
`run_single_check()`, `DOC_DOMAINS`, `IMPL_DOMAINS`,
`DeterministicAuditProvider`, or `knowledge-hub/audit` links anywhere in
`docs/raw/`. Final count: 182 files (185 − 11 deleted + 8 added) — see §8
for the corrected numbers (§1/§6's original ~185/~165 estimates didn't
match what actually needed touching).

**Problem**: ~35 files in `docs/raw/` reference infrastructure being
removed (22 pipeline modules, domain-specific reporting, hardcoded
planners). ~10 more have mixed current/stale content. ~6 have broken
links to a non-existent `knowledge-hub/` directory.

**Reference**: `docs/codebase-refactoring-proposal.md` (what's being
removed), `docs/generic-script-architecture-proposal.md` (what replaces
it).

---

## 1. Audit Summary

| Status | Count | Action |
|--------|-------|--------|
| CURRENT | ~125 | Keep as-is |
| STALE | ~35 | Delete or rewrite |
| MIXED | ~10 | Edit to remove stale parts |
| BROKEN LINKS | ~6 | Fix or remove links |

---

## 2. Files to DELETE (stale, fully replaced by system scripts)

### 2.1 Audit specs (`docs/raw/audit/`) — 18 files

These define domain-specific check IDs (V1-V12, A1-A13, etc.) that the
22 Rust pipeline modules implement. Per `codebase-refactoring-proposal.md`
§3.1, all pipeline modules are being deleted. These specs become the
system's internal concern, not samgraha's documentation.

| File | Check IDs | Replacement |
|------|-----------|-------------|
| `vision-audit.md` | V1-V12 | System's `validate` script |
| `architecture-audit.md` | A1-A13 | System's `validate` script |
| `engineering-audit.md` | E1-E12 | System's `validate` script |
| `feature-audit.md` | F1-F14 | System's `validate` script |
| `feature-design-validation.md` | FD1-FD15 | System's `validate` script |
| `feature-technical-audit.md` | FT1-FT15 | System's `validate` script |
| `coverage-audit.md` | CV1-CV15 | System's `validate` script |
| `security-audit.md` | SEC1-SEC12, SC1-SC11 | System's `validate` script |
| `build-audit.md` | B1-B12, BC1-BC10 | System's `validate` script |
| `consistency-audit.md` | C1-C12 | System's `validate` script |
| `dependency-audit.md` | D1-D8 | System's `validate` script |
| `documentation-structure-audit.md` | SI1-SI7, MC1-MC8, etc. | System's `validate` script |
| `implementation-audit.md` | I1-I15 | System's `validate` script |
| `prototype-audit.md` | P1-P15 | System's `validate` script |
| `readme-audit.md` | R1-R12 | System's `validate` script |
| `design-audit.md` | D1-D12 | System's `validate` script |
| `external-context-audit.md` | EC1-EC12 | System's `validate` script |
| `external-context-ownership-audit.md` | EC1-EC12 | System's `validate` script |
| `deterministic-runtime-audit.md` | S1-S12 | System's `validate` script |

**Why delete, not rewrite**: These specs define checks at the check-ID
level — that level of detail belongs inside a system's `validate` script,
not in samgraha's top-level docs. A system author reads the
`knowledge-system-author-guide.md` to know what scripts to build; the
check IDs are their implementation detail.

### 2.2 Reports (`docs/raw/reports/`) — generated output

| Path | Why stale |
|------|-----------|
| `reports/vision/latest/report.md` | Generated from V1-V12 pipeline |
| `reports/feature/latest/report.md` | Generated from F1-F14 pipeline |
| `reports/architecture/latest/report.md` | Generated from A1-A13 pipeline |
| `reports/*/scorecard/*` | Historical snapshots from old pipeline |

**Why delete**: These are generated artifacts, not source documentation.
In the new architecture, systems generate their own reports via `report`
scripts. These would be regenerated by the system anyway.

---

## 3. Files to REWRITE (stale concepts, current structure)

### 3.1 Feature specs that reference pipeline infrastructure

| File | Stale content | Rewrite approach |
|------|--------------|-----------------|
| `feature/audit-fix.md` | References `run_pipeline()`, `run_single_check()`, pipeline dispatch | Rewrite to describe fix pipeline dispatching to system `validate` scripts |
| `feature/project-planner.md` | References `run_pipeline()` per domain, hardcoded planners | Rewrite to describe `StandardWorkflowPlanner` + system init plans |
| `feature/audit-framework.md` | References "pipeline shall remain provider-independent" | Minor edit: replace "pipeline" with "script dispatch" |

### 3.2 Feature-technical specs

| File | Stale content | Rewrite approach |
|------|--------------|-----------------|
| `feature-technical/project-planner.md` | 4 hardcoded planners, `run_pipeline()`, trait hierarchy | Rewrite to describe `StandardWorkflowPlanner` + `system_plans` table |
| `feature-technical/audit-fix.md` | `run_single_check()`, pipeline dispatch, `reporting.rs` domain logic | Rewrite to describe script-based fix orchestration |
| `feature-technical/audit-framework.md` | `DeterministicAuditProvider` references | Minor edit: describe script dispatch instead |

### 3.3 Product guide audit docs

| File | Stale content | Rewrite approach |
|------|--------------|-----------------|
| `product-guide/audit-guide/stages.md` | `DeterministicAuditProvider`/`SemanticAuditProvider` by name | Rewrite to describe generic provider → script dispatch |
| `product-guide/audit-guide/deterministic.md` | "checks documents against hardcoded rules", check types | Rewrite to describe system-provided `validate` scripts |

---

## 4. Files to EDIT (mixed — remove stale parts, keep current)

### 4.1 Fix broken links (6 files)

All link to `../../../knowledge-hub/audit/semantic/document/*.md` which
doesn't exist. Replace with links to the audit spec in the system's own
`documentation-standards/` directory, or remove the links entirely.

| File | Broken link target |
|------|-------------------|
| `product-guide/concepts/build-audit.md` | `knowledge-hub/.../build-audit.md` |
| `product-guide/concepts/coverage.md` | `knowledge-hub/.../coverage-audit.md` |
| `product-guide/concepts/consistency.md` | `knowledge-hub/.../consistency-audit.md` |
| `product-guide/concepts/security-audit.md` | `knowledge-hub/.../security-audit.md` |
| `product-guide/concepts/dependency.md` | `knowledge-hub/.../dependency-audit.md` |
| `product-guide/tutorials/custom-test-contract.md` | Multiple knowledge-hub links |

**Fix**: Remove the broken links. The audit specs are now system-specific
and live in each system's `documentation-standards/` directory, not in a
shared `knowledge-hub/` path.

### 4.2 Minor edits (mixed content)

| File | Stale part | Fix |
|------|-----------|-----|
| `feature/knowledge-enrichment.md` | "enrichment pipeline" reference | Replace "pipeline" with "script dispatch" |
| `feature/knowledge-compilation.md` | "compilation pipeline" reference | Replace "pipeline" with "compilation service" (generic term) |
| `feature/samagraignore.md` | `pipeline.rs:37-44` reference | Update to reflect new exclusion mechanism |
| `feature-technical/samagraignore.md` | `pipeline.rs` and `discovery.rs` references | Update file paths |
| `product-guide/build-guide/overview.md` | "knowledge-hub loader" reference | Replace with "system scripts" |
| `product-guide/build-guide/distribution.md` | `schema/knowledge-hub/` path reference | Update path |
| `product-guide/concepts/help-audit.md` | `schemas::code_inventory` reference | Verify still accurate, update if needed |
| `product-guide/audit-guide/overview.md` | "Documentation Audit (15 domains)" hardcoded count | Make count dynamic or remove |

---

## 5. Files to ADD (new content needed)

The current docs don't cover the new architecture. Add:

| New file | Purpose |
|----------|---------|
| `docs/raw/feature/capability-dispatch.md` | Feature spec for the capability dispatch system (§8.1-§8.6) |
| `docs/raw/feature-technical/capability-dispatch.md` | Technical design for capability.rs, env.rs, adapter.rs tools |
| `docs/raw/product-guide/concepts/systems.md` | Concept page: what systems are, how they work |
| `docs/raw/product-guide/concepts/scripts.md` | Concept page: capability scripts, discovery, contracts |
| `docs/raw/product-guide/concepts/inheritance.md` | Concept page: system.yaml inheritance model |
| `docs/raw/product-guide/concepts/workflows.md` | Concept page: init plans, phases, dependencies |
| `docs/raw/product-guide/tutorials/first-system.md` | Tutorial: building your first knowledge system |
| `docs/raw/product-guide/mcp-guide/capability-tools.md` | MCP tools reference for run_system_* tools |

---

## 6. Execution Order

### Phase 1: Delete stale files (safe, no content loss)
- Delete 18 audit spec files from `docs/raw/audit/`
- Delete generated reports from `docs/raw/reports/`
- Remove the `docs/raw/audit/` directory (empty after deletion)

### Phase 2: Fix broken links (quick wins)
- Edit 6 product-guide files to remove broken knowledge-hub links

### Phase 3: Edit mixed files (minor content changes)
- Edit 8 files to remove stale pipeline/planner references

### Phase 4: Rewrite stale feature specs (content rewriting)
- Rewrite 5 feature/feature-technical specs to describe script-based architecture

### Phase 5: Add new content (new files)
- Create 8 new docs covering the capability dispatch system

### Phase 6: Verify
- Grep for remaining stale patterns: `run_pipeline`, `run_single_check`, `DOC_DOMAINS`, `IMPL_DOMAINS`, `NewProjectPlanner`, `knowledge-hub/audit`
- Verify no broken internal links
- Verify all new tools are documented

---

## 7. Verification Checklist

After execution — checked against the filesystem, not self-reported:
- [x] `docs/raw/audit/` directory removed — confirmed, no such directory
- [x] `docs/raw/reports/` directory removed — confirmed, no such directory
- [x] No files reference `run_pipeline()` or `run_single_check()` — 0 matches, `grep -rl` across `docs/raw/`
- [x] No files reference `DOC_DOMAINS` or `IMPL_DOMAINS` — 0 matches
- [x] No files reference `NewProjectPlanner` / `DocAuditPlanner` / etc. — 0 matches
- [x] No files reference `DeterministicAuditProvider` by name — 0 matches
- [x] No broken links to `knowledge-hub/audit/` — 0 matches
- [x] New capability dispatch docs exist — all 8 files from §5 confirmed present at their listed paths
- [x] `cargo check -p mcp` still passes — confirmed via full workspace `cargo build`, clean

---

## 8. Impact

| Metric | Before | After (actual, verified) |
|--------|--------|-------|
| Total files in docs/raw/ | ~185 | 182 (11 deleted, 8 added — fewer deletions than §1's ~35-stale estimate; some files were edited/rewritten in place per §3/§4 rather than deleted, which is why the count doesn't match the original ~165 projection) |
| Stale files | ~35 | 0 (verified: 0 `grep` matches for every pattern in §7) |
| Mixed files | ~10 | 0 |
| Broken links | ~6 | 0 |
| New architecture docs | 0 | 8 (all present, verified by path) |
