{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "script/schema/03-security/mitigation-present-at-boundary.schema.json",
  "type": "object",
  "required": ["check", "domain", "category", "status", "metrics", "executed_at", "repo_fingerprint"],
  "properties": {
    "check": { "const": "mitigation-present-at-boundary" },
    "domain": { "const": "03-security" },
    "category": { "const": "B" },
    "status": { "enum": ["pass", "fail", "error", "not_applicable"] },
    "metrics": {
      "type": "object",
      "required": ["mitigations_declared", "mitigations_found", "mitigations_missing"],
      "properties": {
        "mitigations_declared": { "type": "integer", "minimum": 0 },
        "mitigations_found": { "type": "integer", "minimum": 0 },
        "mitigations_missing": { "type": "integer", "minimum": 0 }
      }
    },
    "evidence": { "type": "array", "items": { "type": "string" } },
    "executed_at": { "type": "string", "format": "date-time" },
    "repo_fingerprint": { "type": "string" }
  },
  "if": { "properties": { "status": { "const": "not_applicable" } } },
  "then": { "required": ["evidence"], "properties": { "evidence": { "minItems": 1 } } }
}
