{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "script/schema/14-build/build-succeeds.schema.json",
  "type": "object",
  "required": ["check", "domain", "category", "status", "metrics", "executed_at", "repo_fingerprint"],
  "properties": {
    "check": { "const": "build-succeeds" },
    "domain": { "const": "14-build" },
    "category": { "const": "A" },
    "status": { "enum": ["pass", "fail", "error", "not_applicable"] },
    "metrics": {
      "type": "object",
      "required": ["build_exit_code", "build_duration_seconds"],
      "properties": {
        "build_exit_code": { "type": "integer" },
        "build_duration_seconds": { "type": "number", "minimum": 0 }
      }
    },
    "evidence": { "type": "array", "items": { "type": "string" } },
    "executed_at": { "type": "string", "format": "date-time" },
    "repo_fingerprint": { "type": "string" }
  },
  "if": { "properties": { "status": { "const": "not_applicable" } } },
  "then": { "required": ["evidence"], "properties": { "evidence": { "minItems": 1 } } }
}
