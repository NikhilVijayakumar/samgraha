{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "script/schema/16-product-guide/public-contract-diff.schema.json",
  "type": "object",
  "required": ["check", "domain", "category", "status", "metrics", "executed_at", "repo_fingerprint"],
  "properties": {
    "check": { "const": "public-contract-diff" },
    "domain": { "const": "16-product-guide" },
    "category": { "const": "A" },
    "status": { "enum": ["pass", "fail", "error", "not_applicable"] },
    "metrics": {
      "type": "object",
      "required": ["declared_endpoints", "actual_endpoints", "mismatches"],
      "properties": {
        "declared_endpoints": { "type": "integer", "minimum": 0 },
        "actual_endpoints": { "type": "integer", "minimum": 0 },
        "mismatches": { "type": "integer", "minimum": 0 }
      }
    },
    "evidence": { "type": "array", "items": { "type": "string" } },
    "executed_at": { "type": "string", "format": "date-time" },
    "repo_fingerprint": { "type": "string" }
  },
  "if": { "properties": { "status": { "const": "not_applicable" } } },
  "then": { "required": ["evidence"], "properties": { "evidence": { "minItems": 1 } } }
}
